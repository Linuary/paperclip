# 开发环境配置

PORT=3100
SERVE_UI=true
PAPERCLIP_DEPLOYMENT_MODE=authenticated
PAPERCLIP_DEPLOYMENT_EXPOSURE=private
PAPERCLIP_PUBLIC_URL=https://dev.example.com
PAPERCLIP_STORAGE_PROVIDER=local_disk

# 数据库配置（请替换为实际值）
DATABASE_URL=postgres://paperclip:password@dev-db:5432/paperclip
BETTER_AUTH_SECRET=your-dev-secret
